import React from 'react';

const DirhamIcon = ({ className = "" }: { className?: string }) => {
  return <span className={`text-[0.85em] font-bold ${className}`}>OMR</span>;
};

export default DirhamIcon;
